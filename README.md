Proyecto para la versión 2 de admninistración de tareas:

Frontend en React (JavaSript + SWC + react-router-dom ) 
Backend en NodeJS (MongoDB + Express)

PD: dió fallos a la hora de subir el último frontend, está en la rama frontend.
